package_assets:   on
embed_assets:     off
compress_assets:  on
gzip_assets:      on
javascript_compressor: uglifier
rewrite_relative_paths: off

javascripts:
  main.min:
    - js/application.js

stylesheets:
  main.min:
    - css/main.css
